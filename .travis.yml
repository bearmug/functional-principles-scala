language: java

jdk:
  - oraclejdk8

cache:
 directories:
 - $HOME/.gradle/caches/
 - $HOME/.gradle/wrapper/
 - $HOME/.gradle/nodejs/

install: echo "skip 'gradle assemble' step"
script: gradle build --continue